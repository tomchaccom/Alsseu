# Contributing

## 작업 원칙

- 한 변경은 한 가지 사용자-visible 결과에 집중한다.
- MVP 밖 기능은 이슈로 남기고 함께 구현하지 않는다.
- 사용자 변경이 있는 worktree에서는 관련 파일만 stage한다.
- 커밋 메시지는 한국어 Conventional Commit을 사용한다.

```text
feat : 주간 진행률 대시보드 추가
- 활성 멤버의 merge PR을 5개 슬롯으로 표시
- 마감 후 벌금 대상 목록 제공
```

GitHub 원격 레포의 브랜치/PR 규칙이 연결되면 그 규칙을 단일 기준으로 삼는다. 연결 전에는 에이전트가 브랜치 전략이나 이슈 번호 형식을 임의로 확정하지 않는다.

## PR 체크리스트

- [ ] MVP 범위와 `AGENT.md` 불변식을 지켰다.
- [ ] 비즈니스 규칙 변경에 테스트를 추가했다.
- [ ] UI 변경을 데스크톱/모바일에서 확인했다.
- [ ] `pnpm verify`가 통과한다.
- [ ] 새 환경 변수와 migration 절차를 문서화했다.
